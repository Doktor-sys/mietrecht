apiVersion: apps/v1
kind: Deployment
metadata:
  name: smartlaw-auth-service
  labels:
    app: smartlaw-auth-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: smartlaw-auth-service
  template:
    metadata:
      labels:
        app: smartlaw-auth-service
    spec:
      containers:
      - name: auth-service
        image: smartlaw-auth-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3002
        env:
        - name: NODE_ENV
          value: "production"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: smartlaw-secrets
              key: auth-db-url
        - name: REDIS_URL
          value: "redis://smartlaw-redis:6379"
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: smartlaw-secrets
              key: jwt-secret
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: smartlaw-auth-service
spec:
  selector:
    app: smartlaw-auth-service
  ports:
  - port: 80
    targetPort: 3002
  type: ClusterIP