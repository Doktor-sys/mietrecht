apiVersion: apps/v1
kind: Deployment
metadata:
  name: smartlaw-analytics-service
  labels:
    app: smartlaw-analytics-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: smartlaw-analytics-service
  template:
    metadata:
      labels:
        app: smartlaw-analytics-service
    spec:
      containers:
      - name: analytics-service
        image: smartlaw-analytics-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3009
        env:
        - name: NODE_ENV
          value: "production"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: smartlaw-secrets
              key: analytics-db-url
        - name: REDIS_URL
          value: "redis://smartlaw-redis:6379"
        - name: ELASTICSEARCH_URL
          value: "http://smartlaw-elasticsearch:9200"
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "400m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3009
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3009
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: smartlaw-analytics-service
spec:
  selector:
    app: smartlaw-analytics-service
  ports:
  - port: 80
    targetPort: 3009
  type: ClusterIP