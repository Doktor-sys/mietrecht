apiVersion: apps/v1
kind: Deployment
metadata:
  name: smartlaw-compliance-service
  labels:
    app: smartlaw-compliance-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: smartlaw-compliance-service
  template:
    metadata:
      labels:
        app: smartlaw-compliance-service
    spec:
      containers:
      - name: compliance-service
        image: smartlaw-compliance-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3010
        env:
        - name: NODE_ENV
          value: "production"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: smartlaw-secrets
              key: compliance-db-url
        - name: REDIS_URL
          value: "redis://smartlaw-redis:6379"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3010
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3010
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: smartlaw-compliance-service
spec:
  selector:
    app: smartlaw-compliance-service
  ports:
  - port: 80
    targetPort: 3010
  type: ClusterIP