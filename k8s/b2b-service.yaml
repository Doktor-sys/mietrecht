apiVersion: apps/v1
kind: Deployment
metadata:
  name: smartlaw-b2b-service
  labels:
    app: smartlaw-b2b-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: smartlaw-b2b-service
  template:
    metadata:
      labels:
        app: smartlaw-b2b-service
    spec:
      containers:
      - name: b2b-service
        image: smartlaw-b2b-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3011
        env:
        - name: NODE_ENV
          value: "production"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: smartlaw-secrets
              key: b2b-db-url
        - name: REDIS_URL
          value: "redis://smartlaw-redis:6379"
        - name: MINIO_ENDPOINT
          value: "http://smartlaw-minio:9000"
        - name: MINIO_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: smartlaw-secrets
              key: minio-access-key
        - name: MINIO_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: smartlaw-secrets
              key: minio-secret-key
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "400m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3011
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3011
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: smartlaw-b2b-service
spec:
  selector:
    app: smartlaw-b2b-service
  ports:
  - port: 80
    targetPort: 3011
  type: ClusterIP